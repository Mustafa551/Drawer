import React from 'react';
import {Container, Header} from '../../components';

const CartScreen = () => {
  return (
    <Container>
      <Header title="Filter" />
    </Container>
  );
};

export default CartScreen;
